// Seed script — uncomment when database is configured
// import { prisma } from "../lib/prisma";
// 
// async function main() {
//   console.log("🌱 Seeding database...");
//   ...
// }
// 
// main().then(...).catch(...);

console.log("🌱 Seed script ready. Uncomment and configure DATABASE_URL to run.");
process.exit(0);
